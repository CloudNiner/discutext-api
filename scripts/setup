#!/usr/bin/env bash

set -e

if ! command -v uv &> /dev/null; then
    echo "uv package manager not installed. See https://docs.astral.sh/uv/getting-started/installation/ to continue."
    exit 1
fi

./scripts/update
